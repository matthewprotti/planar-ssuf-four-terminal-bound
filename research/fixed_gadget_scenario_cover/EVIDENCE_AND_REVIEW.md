# Evidence and Review

## Evidence hierarchy

The human-readable proofs are authoritative. Exact programs supply narrower
finite, symbolic, replay, regression, and mutation evidence. Passing code is
not a substitute for an omitted analytic argument.

## Versioned internal dispositions

- R1 v2: GM-002 and GM-003 each `ACCEPT_AS_STATED`; packet
  `ACCEPT_WITH_EDITORIAL_REPAIRS`.
- Public `v0.2.1`: GM-004 is an unrefereed public theorem; Matthew reports no
  mathematical error in that immutable corpus.
- R2 v2: repaired GM-005 `ACCEPT_AS_STATED`; the original R2 unresolved record
  remains immutable history.
- Original R2: GM-006 claim-level `ACCEPT_AS_STATED`.
- R2 v3: repaired GM-008 and GM-009 and the packet each
  `ACCEPT_AS_STATED`, with no blocker and two nonblocking findings,
  `NB-RUNTIME-001` and `NB-EXPOSITION-001`.
- R3A: scenario-cover packet `ACCEPT_WITH_EDITORIAL_REPAIRS`; the five
  nonblocking repairs are applied only in this manifested derived copy.
- R3C / R3D: SC-006 received a mathematics review with nonblocking repairs and
  a separate nonblind exact replay.
- R3B v2: `ACCEPT_AS_STATED` for the technically isolated fixed finite
  reconstruction only.
- R4 v6: S-001 through S-023 and the packet `ACCEPT_AS_STATED`, with no
  finding. This was statement-and-scope integration, not a new proof review.

All of these are internal AI-assisted reviews or replays. R3B v2 used
technical answer isolation, but none is external human review, journal peer
review, academic independence, or formal proof-assistant verification.

## First external human report

One external human report was supplied on 2 August 2026 for the private
candidate. Its raw text is preserved outside the repository with SHA-256
`460f975862a95f2eb5f16d50aed10b835bd6f1bcf4014932afd979f158bdf8c9`.
The supplied text does not identify the reviewer or document
independence/conflicts, so neither may be inferred.

The reviewer reconstructed scenario-cover duality and the short GM-005/GM-006
arguments, checked selected algebra, and reported no mathematical
contradiction in the reconstructible portions. The reviewer did not open the
complete companion proofs that had been left inside the nested source archive,
and therefore marked SC-006 and GM-008/GM-009 unresolved. Those dispositions
are recorded as `NOT_EXAMINED_FULL_PROOF`, not as identified mathematical
counterexamples or refutations.

This report is meaningful external criticism and drove the proof-map,
thirteen-arc envelope, related-work, status, and package-discoverability
repairs. It is not a full claim-by-claim reconstruction, journal peer review,
proof-assistant verification, publication acceptance, or permission to name
the reviewer publicly.

## R3B comparison

The raw comparator `FAIL` is preserved. Every tested mathematical field
matched. After lossless convention normalization, both populations of 451
certificates exact-validated; 14 used alternative valid dual supports at the
same primals and objectives. The precise label is

`SEMANTIC_MATHEMATICAL_MATCH / STRICT_CERTIFICATE_PAYLOAD_IDENTITY_FAIL`.

## Hash-bound private evidence

The compact machine-readable records are `REVIEW_EVIDENCE.json` and
`SOURCE_PROVENANCE.json`; `DERIVATION_NOTES.md` explains the authorized
publication-copy transformations. Raw reviewer prompts, command logs, frozen
run directories, and private orchestration ZIPs are deliberately outside this
candidate repository.
